{
    "docker.commands.attach": "${containerCommand} start ${containerId}; ${containerCommand} attach ${containerId}",
    "black-formatter.args": [
        "--line-length", "180"
    ],
    "containers.commands.attach": "${containerCommand} start ${containerId}; ${containerCommand} attach ${containerId}",
    "python-envs.defaultEnvManager": "ms-python.python:system",
    "python-envs.pythonProjects": [],
}